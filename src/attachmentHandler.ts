// Utility functions for handling Joplin resource links or external URLs in expense tables

export function parseAttachment(cell: string): string | undefined {
    // TODO: Parse markdown links or resource IDs from cell text
    return undefined;
}

export function renderAttachment(link: string): string {
    // TODO: Render markdown or UI element for attachment link
    return link;
}