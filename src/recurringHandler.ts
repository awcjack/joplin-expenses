// Utility functions for recurring expense/income field

export type Recurrence = 'daily' | 'weekly' | 'monthly' | 'yearly' | '';

export function parseRecurrence(cell: string): Recurrence {
    // TODO: Parse recurrence value from cell text
    return '';
}